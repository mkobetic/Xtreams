This is an external stream with built in timeout support. A read operation will signal a Timeout if there is no data available longer than the preconfigured timeout duration. The Timeout exception is resumable which will make the operation continue to wait for another timeout period.

Instance Variables
	timeout	<Duration> how long we're willing to wait for incoming data

