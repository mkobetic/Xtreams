* pool variable references behave unreliably
* the CR value in strings is LF ?
* class side class extensions don't file out (package fileout)
* ifNotNil: doesn't accept an argument
* SharedQueues don't grow (default to size: 50)
